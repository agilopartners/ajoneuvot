<%= form_for(@moto_price) do |f| %>
  <% if @moto_price.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@moto_price.errors.count, "error") %> prohibited this moto_price from being saved:</h2>

      <ul>
      <% @moto_price.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

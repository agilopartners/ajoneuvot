<%= form_for(@boat_asking_price) do |f| %>
  <% if @boat_asking_price.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@boat_asking_price.errors.count, "error") %> prohibited this boat_asking_price from being saved:</h2>

      <ul>
      <% @boat_asking_price.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

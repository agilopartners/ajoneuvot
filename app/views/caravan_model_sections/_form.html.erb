<%= form_for(@caravan_model_section) do |f| %>
  <% if @caravan_model_section.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@caravan_model_section.errors.count, "error") %> prohibited this caravan_model_section from being saved:</h2>

      <ul>
      <% @caravan_model_section.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :caravan_make_section_id %><br />
    <%= f.text_field :caravan_make_section_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

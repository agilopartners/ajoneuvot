<h1>Listing total_owners</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @total_owners.each do |total_owner| %>
  <tr>
    <td><%= total_owner.name %></td>
    <td><%= link_to 'Show', total_owner %></td>
    <td><%= link_to 'Edit', edit_total_owner_path(total_owner) %></td>
    <td><%= link_to 'Destroy', total_owner, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Total owner', new_total_owner_path %>

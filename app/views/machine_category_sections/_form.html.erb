<%= form_for(@machine_category_section) do |f| %>
  <% if @machine_category_section.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@machine_category_section.errors.count, "error") %> prohibited this machine_category_section from being saved:</h2>

      <ul>
      <% @machine_category_section.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :machine_type_id %><br />
    <%= f.text_field :machine_type_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<h1>Listing machine_category_sections</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Machine type</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @machine_category_sections.each do |machine_category_section| %>
  <tr>
    <td><%= machine_category_section.name %></td>
    <td><%= machine_category_section.machine_type_id %></td>
    <td><%= link_to 'Show', machine_category_section %></td>
    <td><%= link_to 'Edit', edit_machine_category_section_path(machine_category_section) %></td>
    <td><%= link_to 'Destroy', machine_category_section, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Machine category section', new_machine_category_section_path %>

<%= form_for(@machine_user_email) do |f| %>
  <% if @machine_user_email.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@machine_user_email.errors.count, "error") %> prohibited this machine_user_email from being saved:</h2>

      <ul>
      <% @machine_user_email.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

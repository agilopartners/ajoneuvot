<%= form_for(@car_acceleration) do |f| %>
  <% if @car_acceleration.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@car_acceleration.errors.count, "error") %> prohibited this car_acceleration from being saved:</h2>

      <ul>
      <% @car_acceleration.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

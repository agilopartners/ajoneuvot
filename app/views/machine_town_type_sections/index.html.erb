<h1>Listing machine_town_type_sections</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Machine domicile type</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @machine_town_type_sections.each do |machine_town_type_section| %>
  <tr>
    <td><%= machine_town_type_section.name %></td>
    <td><%= machine_town_type_section.Machine_Domicile_type_id %></td>
    <td><%= link_to 'Show', machine_town_type_section %></td>
    <td><%= link_to 'Edit', edit_machine_town_type_section_path(machine_town_type_section) %></td>
    <td><%= link_to 'Destroy', machine_town_type_section, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Machine town type section', new_machine_town_type_section_path %>

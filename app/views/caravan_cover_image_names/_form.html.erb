<%= form_for(@caravan_cover_image_name) do |f| %>
  <% if @caravan_cover_image_name.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@caravan_cover_image_name.errors.count, "error") %> prohibited this caravan_cover_image_name from being saved:</h2>

      <ul>
      <% @caravan_cover_image_name.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

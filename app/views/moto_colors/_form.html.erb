<%= form_for(@moto_color) do |f| %>
  <% if @moto_color.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@moto_color.errors.count, "error") %> prohibited this moto_color from being saved:</h2>

      <ul>
      <% @moto_color.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<h1>Listing moto_colors</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @moto_colors.each do |moto_color| %>
  <tr>
    <td><%= moto_color.name %></td>
    <td><%= link_to 'Show', moto_color %></td>
    <td><%= link_to 'Edit', edit_moto_color_path(moto_color) %></td>
    <td><%= link_to 'Destroy', moto_color, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Moto color', new_moto_color_path %>

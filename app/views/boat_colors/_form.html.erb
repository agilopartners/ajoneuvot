<%= form_for(@boat_color) do |f| %>
  <% if @boat_color.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@boat_color.errors.count, "error") %> prohibited this boat_color from being saved:</h2>

      <ul>
      <% @boat_color.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= form_for(@boat_height) do |f| %>
  <% if @boat_height.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@boat_height.errors.count, "error") %> prohibited this boat_height from being saved:</h2>

      <ul>
      <% @boat_height.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= form_for(@machine_make_section) do |f| %>
  <% if @machine_make_section.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@machine_make_section.errors.count, "error") %> prohibited this machine_make_section from being saved:</h2>

      <ul>
      <% @machine_make_section.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

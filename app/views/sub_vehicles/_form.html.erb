<%= form_for(@sub_vehicle) do |f| %>
  <% if @sub_vehicle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sub_vehicle.errors.count, "error") %> prohibited this sub_vehicle from being saved:</h2>

      <ul>
      <% @sub_vehicle.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :vehicle_id %><br />
    <%= f.text_field :vehicle_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

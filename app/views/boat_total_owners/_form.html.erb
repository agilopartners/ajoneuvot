<%= form_for(@boat_total_owner) do |f| %>
  <% if @boat_total_owner.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@boat_total_owner.errors.count, "error") %> prohibited this boat_total_owner from being saved:</h2>

      <ul>
      <% @boat_total_owner.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<h1>Listing boat_total_owners</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @boat_total_owners.each do |boat_total_owner| %>
  <tr>
    <td><%= boat_total_owner.name %></td>
    <td><%= link_to 'Show', boat_total_owner %></td>
    <td><%= link_to 'Edit', edit_boat_total_owner_path(boat_total_owner) %></td>
    <td><%= link_to 'Destroy', boat_total_owner, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Boat total owner', new_boat_total_owner_path %>

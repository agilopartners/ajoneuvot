<%= form_for(@moto_drive_type) do |f| %>
  <% if @moto_drive_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@moto_drive_type.errors.count, "error") %> prohibited this moto_drive_type from being saved:</h2>

      <ul>
      <% @moto_drive_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= form_for(@car_user_name) do |f| %>
  <% if @car_user_name.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@car_user_name.errors.count, "error") %> prohibited this car_user_name from being saved:</h2>

      <ul>
      <% @car_user_name.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

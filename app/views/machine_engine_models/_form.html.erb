<%= form_for(@machine_engine_model) do |f| %>
  <% if @machine_engine_model.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@machine_engine_model.errors.count, "error") %> prohibited this machine_engine_model from being saved:</h2>

      <ul>
      <% @machine_engine_model.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

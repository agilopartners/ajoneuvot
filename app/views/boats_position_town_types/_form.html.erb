<%= form_for(@boats_position_town_type) do |f| %>
  <% if @boats_position_town_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@boats_position_town_type.errors.count, "error") %> prohibited this boats_position_town_type from being saved:</h2>

      <ul>
      <% @boats_position_town_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= form_for(@moto_registration_number) do |f| %>
  <% if @moto_registration_number.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@moto_registration_number.errors.count, "error") %> prohibited this moto_registration_number from being saved:</h2>

      <ul>
      <% @moto_registration_number.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

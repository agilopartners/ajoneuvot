<%= form_for(@moto_user_phone) do |f| %>
  <% if @moto_user_phone.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@moto_user_phone.errors.count, "error") %> prohibited this moto_user_phone from being saved:</h2>

      <ul>
      <% @moto_user_phone.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

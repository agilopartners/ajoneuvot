<h1>Listing moto_user_phones</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @moto_user_phones.each do |moto_user_phone| %>
  <tr>
    <td><%= moto_user_phone.name %></td>
    <td><%= link_to 'Show', moto_user_phone %></td>
    <td><%= link_to 'Edit', edit_moto_user_phone_path(moto_user_phone) %></td>
    <td><%= link_to 'Destroy', moto_user_phone, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Moto user phone', new_moto_user_phone_path %>

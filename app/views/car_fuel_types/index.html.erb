<h1>Listing car_fuel_types</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @car_fuel_types.each do |car_fuel_type| %>
  <tr>
    <td><%= car_fuel_type.name %></td>
    <td><%= link_to 'Show', car_fuel_type %></td>
    <td><%= link_to 'Edit', edit_car_fuel_type_path(car_fuel_type) %></td>
    <td><%= link_to 'Destroy', car_fuel_type, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Car fuel type', new_car_fuel_type_path %>

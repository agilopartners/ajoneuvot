<%= form_for(@car_fuel_type) do |f| %>
  <% if @car_fuel_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@car_fuel_type.errors.count, "error") %> prohibited this car_fuel_type from being saved:</h2>

      <ul>
      <% @car_fuel_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

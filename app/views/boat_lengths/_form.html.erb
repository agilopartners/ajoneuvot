<%= form_for(@boat_length) do |f| %>
  <% if @boat_length.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@boat_length.errors.count, "error") %> prohibited this boat_length from being saved:</h2>

      <ul>
      <% @boat_length.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

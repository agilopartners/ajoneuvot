<h1>Listing car_drive_types</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @car_drive_types.each do |car_drive_type| %>
  <tr>
    <td><%= car_drive_type.name %></td>
    <td><%= link_to 'Show', car_drive_type %></td>
    <td><%= link_to 'Edit', edit_car_drive_type_path(car_drive_type) %></td>
    <td><%= link_to 'Destroy', car_drive_type, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Car drive type', new_car_drive_type_path %>

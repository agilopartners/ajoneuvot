<%= form_for(@car_price) do |f| %>
  <% if @car_price.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@car_price.errors.count, "error") %> prohibited this car_price from being saved:</h2>

      <ul>
      <% @car_price.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

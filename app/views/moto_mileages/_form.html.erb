<%= form_for(@moto_mileage) do |f| %>
  <% if @moto_mileage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@moto_mileage.errors.count, "error") %> prohibited this moto_mileage from being saved:</h2>

      <ul>
      <% @moto_mileage.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

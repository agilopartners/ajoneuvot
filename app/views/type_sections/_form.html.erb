<%= form_for(@type_section) do |f| %>
  <% if @type_section.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@type_section.errors.count, "error") %> prohibited this type_section from being saved:</h2>

      <ul>
      <% @type_section.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :vehicle_class_id %><br />
    <%= f.text_field :vehicle_class_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<h1>Listing car_colors</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @car_colors.each do |car_color| %>
  <tr>
    <td><%= car_color.name %></td>
    <td><%= link_to 'Show', car_color %></td>
    <td><%= link_to 'Edit', edit_car_color_path(car_color) %></td>
    <td><%= link_to 'Destroy', car_color, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Car color', new_car_color_path %>

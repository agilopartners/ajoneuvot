<%= form_for(@boat_body_material) do |f| %>
  <% if @boat_body_material.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@boat_body_material.errors.count, "error") %> prohibited this boat_body_material from being saved:</h2>

      <ul>
      <% @boat_body_material.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

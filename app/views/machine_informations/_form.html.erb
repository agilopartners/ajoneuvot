<%= form_for(@machine_information) do |f| %>
  <% if @machine_information.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@machine_information.errors.count, "error") %> prohibited this machine_information from being saved:</h2>

      <ul>
      <% @machine_information.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_area :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

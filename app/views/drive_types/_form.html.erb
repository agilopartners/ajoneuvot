<%= form_for(@drive_type) do |f| %>
  <% if @drive_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@drive_type.errors.count, "error") %> prohibited this drive_type from being saved:</h2>

      <ul>
      <% @drive_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<h1>Listing car_engine_sizes</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @car_engine_sizes.each do |car_engine_size| %>
  <tr>
    <td><%= car_engine_size.name %></td>
    <td><%= link_to 'Show', car_engine_size %></td>
    <td><%= link_to 'Edit', edit_car_engine_size_path(car_engine_size) %></td>
    <td><%= link_to 'Destroy', car_engine_size, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Car engine size', new_car_engine_size_path %>

<%= form_for(@moto_town_type) do |f| %>
  <% if @moto_town_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@moto_town_type.errors.count, "error") %> prohibited this moto_town_type from being saved:</h2>

      <ul>
      <% @moto_town_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :moto_domicile_type_id %><br />
    <%= f.text_field :moto_domicile_type_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

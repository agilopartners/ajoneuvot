<%= form_for(@car_weight) do |f| %>
  <% if @car_weight.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@car_weight.errors.count, "error") %> prohibited this car_weight from being saved:</h2>

      <ul>
      <% @car_weight.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

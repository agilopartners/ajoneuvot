<h1>Listing cover_image_uids</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @cover_image_uids.each do |cover_image_uid| %>
  <tr>
    <td><%= cover_image_uid.name %></td>
    <td><%= link_to 'Show', cover_image_uid %></td>
    <td><%= link_to 'Edit', edit_cover_image_uid_path(cover_image_uid) %></td>
    <td><%= link_to 'Destroy', cover_image_uid, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Cover image uid', new_cover_image_uid_path %>

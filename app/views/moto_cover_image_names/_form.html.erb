<%= form_for(@moto_cover_image_name) do |f| %>
  <% if @moto_cover_image_name.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@moto_cover_image_name.errors.count, "error") %> prohibited this moto_cover_image_name from being saved:</h2>

      <ul>
      <% @moto_cover_image_name.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

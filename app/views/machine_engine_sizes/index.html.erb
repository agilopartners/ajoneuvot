<h1>Listing machine_engine_sizes</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @machine_engine_sizes.each do |machine_engine_size| %>
  <tr>
    <td><%= machine_engine_size.name %></td>
    <td><%= link_to 'Show', machine_engine_size %></td>
    <td><%= link_to 'Edit', edit_machine_engine_size_path(machine_engine_size) %></td>
    <td><%= link_to 'Destroy', machine_engine_size, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Machine engine size', new_machine_engine_size_path %>

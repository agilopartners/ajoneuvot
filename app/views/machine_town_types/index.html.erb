<h1>Listing machine_town_types</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Machine domicile type</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @machine_town_types.each do |machine_town_type| %>
  <tr>
    <td><%= machine_town_type.name %></td>
    <td><%= machine_town_type.machine_domicile_type_id %></td>
    <td><%= link_to 'Show', machine_town_type %></td>
    <td><%= link_to 'Edit', edit_machine_town_type_path(machine_town_type) %></td>
    <td><%= link_to 'Destroy', machine_town_type, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Machine town type', new_machine_town_type_path %>

<%= form_for(@caravan_lenght) do |f| %>
  <% if @caravan_lenght.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@caravan_lenght.errors.count, "error") %> prohibited this caravan_lenght from being saved:</h2>

      <ul>
      <% @caravan_lenght.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

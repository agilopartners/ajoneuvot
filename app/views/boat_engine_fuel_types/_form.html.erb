<%= form_for(@boat_engine_fuel_type) do |f| %>
  <% if @boat_engine_fuel_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@boat_engine_fuel_type.errors.count, "error") %> prohibited this boat_engine_fuel_type from being saved:</h2>

      <ul>
      <% @boat_engine_fuel_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<h1>Listing boat_engine_fuel_types</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @boat_engine_fuel_types.each do |boat_engine_fuel_type| %>
  <tr>
    <td><%= boat_engine_fuel_type.name %></td>
    <td><%= link_to 'Show', boat_engine_fuel_type %></td>
    <td><%= link_to 'Edit', edit_boat_engine_fuel_type_path(boat_engine_fuel_type) %></td>
    <td><%= link_to 'Destroy', boat_engine_fuel_type, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Boat engine fuel type', new_boat_engine_fuel_type_path %>

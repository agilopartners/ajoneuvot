<%= form_for(@moto_information) do |f| %>
  <% if @moto_information.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@moto_information.errors.count, "error") %> prohibited this moto_information from being saved:</h2>

      <ul>
      <% @moto_information.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_area :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

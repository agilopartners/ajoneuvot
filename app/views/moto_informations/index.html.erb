<h1>Listing moto_informations</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @moto_informations.each do |moto_information| %>
  <tr>
    <td><%= moto_information.name %></td>
    <td><%= link_to 'Show', moto_information %></td>
    <td><%= link_to 'Edit', edit_moto_information_path(moto_information) %></td>
    <td><%= link_to 'Destroy', moto_information, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Moto information', new_moto_information_path %>

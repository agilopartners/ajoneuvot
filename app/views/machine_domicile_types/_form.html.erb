<%= form_for(@machine_domicile_type) do |f| %>
  <% if @machine_domicile_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@machine_domicile_type.errors.count, "error") %> prohibited this machine_domicile_type from being saved:</h2>

      <ul>
      <% @machine_domicile_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

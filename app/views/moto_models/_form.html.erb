<%= form_for(@moto_model) do |f| %>
  <% if @moto_model.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@moto_model.errors.count, "error") %> prohibited this moto_model from being saved:</h2>

      <ul>
      <% @moto_model.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :moto_make_id %><br />
    <%= f.text_field :moto_make_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

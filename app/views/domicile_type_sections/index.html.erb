<h1>Listing domicile_type_sections</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Boats position town type</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @domicile_type_sections.each do |domicile_type_section| %>
  <tr>
    <td><%= domicile_type_section.name %></td>
    <td><%= domicile_type_section.boats_position_town_type_id %></td>
    <td><%= link_to 'Show', domicile_type_section %></td>
    <td><%= link_to 'Edit', edit_domicile_type_section_path(domicile_type_section) %></td>
    <td><%= link_to 'Destroy', domicile_type_section, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Domicile type section', new_domicile_type_section_path %>

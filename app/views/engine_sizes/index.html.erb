<h1>Listing engine_sizes</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @engine_sizes.each do |engine_size| %>
  <tr>
    <td><%= engine_size.name %></td>
    <td><%= link_to 'Show', engine_size %></td>
    <td><%= link_to 'Edit', edit_engine_size_path(engine_size) %></td>
    <td><%= link_to 'Destroy', engine_size, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Engine size', new_engine_size_path %>

<%= form_for(@engine_size) do |f| %>
  <% if @engine_size.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@engine_size.errors.count, "error") %> prohibited this engine_size from being saved:</h2>

      <ul>
      <% @engine_size.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

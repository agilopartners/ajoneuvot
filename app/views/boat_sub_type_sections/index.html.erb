<h1>Listing boat_sub_type_sections</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Boat type</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @boat_sub_type_sections.each do |boat_sub_type_section| %>
  <tr>
    <td><%= boat_sub_type_section.name %></td>
    <td><%= boat_sub_type_section.boat_type_id %></td>
    <td><%= link_to 'Show', boat_sub_type_section %></td>
    <td><%= link_to 'Edit', edit_boat_sub_type_section_path(boat_sub_type_section) %></td>
    <td><%= link_to 'Destroy', boat_sub_type_section, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Boat sub type section', new_boat_sub_type_section_path %>

<%= form_for(@caravan_user_email) do |f| %>
  <% if @caravan_user_email.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@caravan_user_email.errors.count, "error") %> prohibited this caravan_user_email from being saved:</h2>

      <ul>
      <% @caravan_user_email.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<h1>Listing boat_engine_makes</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @boat_engine_makes.each do |boat_engine_make| %>
  <tr>
    <td><%= boat_engine_make.name %></td>
    <td><%= link_to 'Show', boat_engine_make %></td>
    <td><%= link_to 'Edit', edit_boat_engine_make_path(boat_engine_make) %></td>
    <td><%= link_to 'Destroy', boat_engine_make, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Boat engine make', new_boat_engine_make_path %>

<%= form_for(@gearbox_type) do |f| %>
  <% if @gearbox_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@gearbox_type.errors.count, "error") %> prohibited this gearbox_type from being saved:</h2>

      <ul>
      <% @gearbox_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

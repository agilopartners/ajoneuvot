<h1>Listing caravan_types</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @caravan_types.each do |caravan_type| %>
  <tr>
    <td><%= caravan_type.name %></td>
    <td><%= link_to 'Show', caravan_type %></td>
    <td><%= link_to 'Edit', edit_caravan_type_path(caravan_type) %></td>
    <td><%= link_to 'Destroy', caravan_type, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Caravan type', new_caravan_type_path %>

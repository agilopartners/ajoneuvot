<%= form_for(@machine_price) do |f| %>
  <% if @machine_price.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@machine_price.errors.count, "error") %> prohibited this machine_price from being saved:</h2>

      <ul>
      <% @machine_price.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

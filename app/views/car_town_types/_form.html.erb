<%= form_for(@car_town_type) do |f| %>
  <% if @car_town_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@car_town_type.errors.count, "error") %> prohibited this car_town_type from being saved:</h2>

      <ul>
      <% @car_town_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :car_domicile_type_id %><br />
    <%= f.text_field :car_domicile_type_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

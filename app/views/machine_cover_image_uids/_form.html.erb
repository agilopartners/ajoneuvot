<%= form_for(@machine_cover_image_uid) do |f| %>
  <% if @machine_cover_image_uid.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@machine_cover_image_uid.errors.count, "error") %> prohibited this machine_cover_image_uid from being saved:</h2>

      <ul>
      <% @machine_cover_image_uid.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

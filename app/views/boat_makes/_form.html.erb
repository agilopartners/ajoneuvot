<%= form_for(@boat_make) do |f| %>
  <% if @boat_make.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@boat_make.errors.count, "error") %> prohibited this boat_make from being saved:</h2>

      <ul>
      <% @boat_make.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :boat_type_id %><br />
    <%= f.text_field :boat_type_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

require 'spec_helper'

# This spec was generated by rspec-rails when you ran the scaffold generator.
# It demonstrates how one might use RSpec to specify the controller code that
# was generated by the Rails when you ran the scaffold generator.

describe MotoYearModelsController do

  def mock_moto_year_model(stubs={})
    @mock_moto_year_model ||= mock_model(MotoYearModel, stubs).as_null_object
  end

  describe "GET index" do
    it "assigns all moto_year_models as @moto_year_models" do
      MotoYearModel.stub(:all) { [mock_moto_year_model] }
      get :index
      assigns(:moto_year_models).should eq([mock_moto_year_model])
    end
  end

  describe "GET show" do
    it "assigns the requested moto_year_model as @moto_year_model" do
      MotoYearModel.stub(:find).with("37") { mock_moto_year_model }
      get :show, :id => "37"
      assigns(:moto_year_model).should be(mock_moto_year_model)
    end
  end

  describe "GET new" do
    it "assigns a new moto_year_model as @moto_year_model" do
      MotoYearModel.stub(:new) { mock_moto_year_model }
      get :new
      assigns(:moto_year_model).should be(mock_moto_year_model)
    end
  end

  describe "GET edit" do
    it "assigns the requested moto_year_model as @moto_year_model" do
      MotoYearModel.stub(:find).with("37") { mock_moto_year_model }
      get :edit, :id => "37"
      assigns(:moto_year_model).should be(mock_moto_year_model)
    end
  end

  describe "POST create" do
    describe "with valid params" do
      it "assigns a newly created moto_year_model as @moto_year_model" do
        MotoYearModel.stub(:new).with({'these' => 'params'}) { mock_moto_year_model(:save => true) }
        post :create, :moto_year_model => {'these' => 'params'}
        assigns(:moto_year_model).should be(mock_moto_year_model)
      end

      it "redirects to the created moto_year_model" do
        MotoYearModel.stub(:new) { mock_moto_year_model(:save => true) }
        post :create, :moto_year_model => {}
        response.should redirect_to(moto_year_model_url(mock_moto_year_model))
      end
    end

    describe "with invalid params" do
      it "assigns a newly created but unsaved moto_year_model as @moto_year_model" do
        MotoYearModel.stub(:new).with({'these' => 'params'}) { mock_moto_year_model(:save => false) }
        post :create, :moto_year_model => {'these' => 'params'}
        assigns(:moto_year_model).should be(mock_moto_year_model)
      end

      it "re-renders the 'new' template" do
        MotoYearModel.stub(:new) { mock_moto_year_model(:save => false) }
        post :create, :moto_year_model => {}
        response.should render_template("new")
      end
    end
  end

  describe "PUT update" do
    describe "with valid params" do
      it "updates the requested moto_year_model" do
        MotoYearModel.stub(:find).with("37") { mock_moto_year_model }
        mock_moto_year_model.should_receive(:update_attributes).with({'these' => 'params'})
        put :update, :id => "37", :moto_year_model => {'these' => 'params'}
      end

      it "assigns the requested moto_year_model as @moto_year_model" do
        MotoYearModel.stub(:find) { mock_moto_year_model(:update_attributes => true) }
        put :update, :id => "1"
        assigns(:moto_year_model).should be(mock_moto_year_model)
      end

      it "redirects to the moto_year_model" do
        MotoYearModel.stub(:find) { mock_moto_year_model(:update_attributes => true) }
        put :update, :id => "1"
        response.should redirect_to(moto_year_model_url(mock_moto_year_model))
      end
    end

    describe "with invalid params" do
      it "assigns the moto_year_model as @moto_year_model" do
        MotoYearModel.stub(:find) { mock_moto_year_model(:update_attributes => false) }
        put :update, :id => "1"
        assigns(:moto_year_model).should be(mock_moto_year_model)
      end

      it "re-renders the 'edit' template" do
        MotoYearModel.stub(:find) { mock_moto_year_model(:update_attributes => false) }
        put :update, :id => "1"
        response.should render_template("edit")
      end
    end
  end

  describe "DELETE destroy" do
    it "destroys the requested moto_year_model" do
      MotoYearModel.stub(:find).with("37") { mock_moto_year_model }
      mock_moto_year_model.should_receive(:destroy)
      delete :destroy, :id => "37"
    end

    it "redirects to the moto_year_models list" do
      MotoYearModel.stub(:find) { mock_moto_year_model }
      delete :destroy, :id => "1"
      response.should redirect_to(moto_year_models_url)
    end
  end

end

require 'spec_helper'

# This spec was generated by rspec-rails when you ran the scaffold generator.
# It demonstrates how one might use RSpec to specify the controller code that
# was generated by the Rails when you ran the scaffold generator.

describe SubtypeSectionsController do

  def mock_subtype_section(stubs={})
    @mock_subtype_section ||= mock_model(SubtypeSection, stubs).as_null_object
  end

  describe "GET index" do
    it "assigns all subtype_sections as @subtype_sections" do
      SubtypeSection.stub(:all) { [mock_subtype_section] }
      get :index
      assigns(:subtype_sections).should eq([mock_subtype_section])
    end
  end

  describe "GET show" do
    it "assigns the requested subtype_section as @subtype_section" do
      SubtypeSection.stub(:find).with("37") { mock_subtype_section }
      get :show, :id => "37"
      assigns(:subtype_section).should be(mock_subtype_section)
    end
  end

  describe "GET new" do
    it "assigns a new subtype_section as @subtype_section" do
      SubtypeSection.stub(:new) { mock_subtype_section }
      get :new
      assigns(:subtype_section).should be(mock_subtype_section)
    end
  end

  describe "GET edit" do
    it "assigns the requested subtype_section as @subtype_section" do
      SubtypeSection.stub(:find).with("37") { mock_subtype_section }
      get :edit, :id => "37"
      assigns(:subtype_section).should be(mock_subtype_section)
    end
  end

  describe "POST create" do
    describe "with valid params" do
      it "assigns a newly created subtype_section as @subtype_section" do
        SubtypeSection.stub(:new).with({'these' => 'params'}) { mock_subtype_section(:save => true) }
        post :create, :subtype_section => {'these' => 'params'}
        assigns(:subtype_section).should be(mock_subtype_section)
      end

      it "redirects to the created subtype_section" do
        SubtypeSection.stub(:new) { mock_subtype_section(:save => true) }
        post :create, :subtype_section => {}
        response.should redirect_to(subtype_section_url(mock_subtype_section))
      end
    end

    describe "with invalid params" do
      it "assigns a newly created but unsaved subtype_section as @subtype_section" do
        SubtypeSection.stub(:new).with({'these' => 'params'}) { mock_subtype_section(:save => false) }
        post :create, :subtype_section => {'these' => 'params'}
        assigns(:subtype_section).should be(mock_subtype_section)
      end

      it "re-renders the 'new' template" do
        SubtypeSection.stub(:new) { mock_subtype_section(:save => false) }
        post :create, :subtype_section => {}
        response.should render_template("new")
      end
    end
  end

  describe "PUT update" do
    describe "with valid params" do
      it "updates the requested subtype_section" do
        SubtypeSection.stub(:find).with("37") { mock_subtype_section }
        mock_subtype_section.should_receive(:update_attributes).with({'these' => 'params'})
        put :update, :id => "37", :subtype_section => {'these' => 'params'}
      end

      it "assigns the requested subtype_section as @subtype_section" do
        SubtypeSection.stub(:find) { mock_subtype_section(:update_attributes => true) }
        put :update, :id => "1"
        assigns(:subtype_section).should be(mock_subtype_section)
      end

      it "redirects to the subtype_section" do
        SubtypeSection.stub(:find) { mock_subtype_section(:update_attributes => true) }
        put :update, :id => "1"
        response.should redirect_to(subtype_section_url(mock_subtype_section))
      end
    end

    describe "with invalid params" do
      it "assigns the subtype_section as @subtype_section" do
        SubtypeSection.stub(:find) { mock_subtype_section(:update_attributes => false) }
        put :update, :id => "1"
        assigns(:subtype_section).should be(mock_subtype_section)
      end

      it "re-renders the 'edit' template" do
        SubtypeSection.stub(:find) { mock_subtype_section(:update_attributes => false) }
        put :update, :id => "1"
        response.should render_template("edit")
      end
    end
  end

  describe "DELETE destroy" do
    it "destroys the requested subtype_section" do
      SubtypeSection.stub(:find).with("37") { mock_subtype_section }
      mock_subtype_section.should_receive(:destroy)
      delete :destroy, :id => "37"
    end

    it "redirects to the subtype_sections list" do
      SubtypeSection.stub(:find) { mock_subtype_section }
      delete :destroy, :id => "1"
      response.should redirect_to(subtype_sections_url)
    end
  end

end

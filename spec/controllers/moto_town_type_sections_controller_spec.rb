require 'spec_helper'

# This spec was generated by rspec-rails when you ran the scaffold generator.
# It demonstrates how one might use RSpec to specify the controller code that
# was generated by the Rails when you ran the scaffold generator.

describe MotoTownTypeSectionsController do

  def mock_moto_town_type_section(stubs={})
    @mock_moto_town_type_section ||= mock_model(MotoTownTypeSection, stubs).as_null_object
  end

  describe "GET index" do
    it "assigns all moto_town_type_sections as @moto_town_type_sections" do
      MotoTownTypeSection.stub(:all) { [mock_moto_town_type_section] }
      get :index
      assigns(:moto_town_type_sections).should eq([mock_moto_town_type_section])
    end
  end

  describe "GET show" do
    it "assigns the requested moto_town_type_section as @moto_town_type_section" do
      MotoTownTypeSection.stub(:find).with("37") { mock_moto_town_type_section }
      get :show, :id => "37"
      assigns(:moto_town_type_section).should be(mock_moto_town_type_section)
    end
  end

  describe "GET new" do
    it "assigns a new moto_town_type_section as @moto_town_type_section" do
      MotoTownTypeSection.stub(:new) { mock_moto_town_type_section }
      get :new
      assigns(:moto_town_type_section).should be(mock_moto_town_type_section)
    end
  end

  describe "GET edit" do
    it "assigns the requested moto_town_type_section as @moto_town_type_section" do
      MotoTownTypeSection.stub(:find).with("37") { mock_moto_town_type_section }
      get :edit, :id => "37"
      assigns(:moto_town_type_section).should be(mock_moto_town_type_section)
    end
  end

  describe "POST create" do
    describe "with valid params" do
      it "assigns a newly created moto_town_type_section as @moto_town_type_section" do
        MotoTownTypeSection.stub(:new).with({'these' => 'params'}) { mock_moto_town_type_section(:save => true) }
        post :create, :moto_town_type_section => {'these' => 'params'}
        assigns(:moto_town_type_section).should be(mock_moto_town_type_section)
      end

      it "redirects to the created moto_town_type_section" do
        MotoTownTypeSection.stub(:new) { mock_moto_town_type_section(:save => true) }
        post :create, :moto_town_type_section => {}
        response.should redirect_to(moto_town_type_section_url(mock_moto_town_type_section))
      end
    end

    describe "with invalid params" do
      it "assigns a newly created but unsaved moto_town_type_section as @moto_town_type_section" do
        MotoTownTypeSection.stub(:new).with({'these' => 'params'}) { mock_moto_town_type_section(:save => false) }
        post :create, :moto_town_type_section => {'these' => 'params'}
        assigns(:moto_town_type_section).should be(mock_moto_town_type_section)
      end

      it "re-renders the 'new' template" do
        MotoTownTypeSection.stub(:new) { mock_moto_town_type_section(:save => false) }
        post :create, :moto_town_type_section => {}
        response.should render_template("new")
      end
    end
  end

  describe "PUT update" do
    describe "with valid params" do
      it "updates the requested moto_town_type_section" do
        MotoTownTypeSection.stub(:find).with("37") { mock_moto_town_type_section }
        mock_moto_town_type_section.should_receive(:update_attributes).with({'these' => 'params'})
        put :update, :id => "37", :moto_town_type_section => {'these' => 'params'}
      end

      it "assigns the requested moto_town_type_section as @moto_town_type_section" do
        MotoTownTypeSection.stub(:find) { mock_moto_town_type_section(:update_attributes => true) }
        put :update, :id => "1"
        assigns(:moto_town_type_section).should be(mock_moto_town_type_section)
      end

      it "redirects to the moto_town_type_section" do
        MotoTownTypeSection.stub(:find) { mock_moto_town_type_section(:update_attributes => true) }
        put :update, :id => "1"
        response.should redirect_to(moto_town_type_section_url(mock_moto_town_type_section))
      end
    end

    describe "with invalid params" do
      it "assigns the moto_town_type_section as @moto_town_type_section" do
        MotoTownTypeSection.stub(:find) { mock_moto_town_type_section(:update_attributes => false) }
        put :update, :id => "1"
        assigns(:moto_town_type_section).should be(mock_moto_town_type_section)
      end

      it "re-renders the 'edit' template" do
        MotoTownTypeSection.stub(:find) { mock_moto_town_type_section(:update_attributes => false) }
        put :update, :id => "1"
        response.should render_template("edit")
      end
    end
  end

  describe "DELETE destroy" do
    it "destroys the requested moto_town_type_section" do
      MotoTownTypeSection.stub(:find).with("37") { mock_moto_town_type_section }
      mock_moto_town_type_section.should_receive(:destroy)
      delete :destroy, :id => "37"
    end

    it "redirects to the moto_town_type_sections list" do
      MotoTownTypeSection.stub(:find) { mock_moto_town_type_section }
      delete :destroy, :id => "1"
      response.should redirect_to(moto_town_type_sections_url)
    end
  end

end

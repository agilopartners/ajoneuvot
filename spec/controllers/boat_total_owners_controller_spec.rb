require 'spec_helper'

# This spec was generated by rspec-rails when you ran the scaffold generator.
# It demonstrates how one might use RSpec to specify the controller code that
# was generated by the Rails when you ran the scaffold generator.

describe BoatTotalOwnersController do

  def mock_boat_total_owner(stubs={})
    @mock_boat_total_owner ||= mock_model(BoatTotalOwner, stubs).as_null_object
  end

  describe "GET index" do
    it "assigns all boat_total_owners as @boat_total_owners" do
      BoatTotalOwner.stub(:all) { [mock_boat_total_owner] }
      get :index
      assigns(:boat_total_owners).should eq([mock_boat_total_owner])
    end
  end

  describe "GET show" do
    it "assigns the requested boat_total_owner as @boat_total_owner" do
      BoatTotalOwner.stub(:find).with("37") { mock_boat_total_owner }
      get :show, :id => "37"
      assigns(:boat_total_owner).should be(mock_boat_total_owner)
    end
  end

  describe "GET new" do
    it "assigns a new boat_total_owner as @boat_total_owner" do
      BoatTotalOwner.stub(:new) { mock_boat_total_owner }
      get :new
      assigns(:boat_total_owner).should be(mock_boat_total_owner)
    end
  end

  describe "GET edit" do
    it "assigns the requested boat_total_owner as @boat_total_owner" do
      BoatTotalOwner.stub(:find).with("37") { mock_boat_total_owner }
      get :edit, :id => "37"
      assigns(:boat_total_owner).should be(mock_boat_total_owner)
    end
  end

  describe "POST create" do
    describe "with valid params" do
      it "assigns a newly created boat_total_owner as @boat_total_owner" do
        BoatTotalOwner.stub(:new).with({'these' => 'params'}) { mock_boat_total_owner(:save => true) }
        post :create, :boat_total_owner => {'these' => 'params'}
        assigns(:boat_total_owner).should be(mock_boat_total_owner)
      end

      it "redirects to the created boat_total_owner" do
        BoatTotalOwner.stub(:new) { mock_boat_total_owner(:save => true) }
        post :create, :boat_total_owner => {}
        response.should redirect_to(boat_total_owner_url(mock_boat_total_owner))
      end
    end

    describe "with invalid params" do
      it "assigns a newly created but unsaved boat_total_owner as @boat_total_owner" do
        BoatTotalOwner.stub(:new).with({'these' => 'params'}) { mock_boat_total_owner(:save => false) }
        post :create, :boat_total_owner => {'these' => 'params'}
        assigns(:boat_total_owner).should be(mock_boat_total_owner)
      end

      it "re-renders the 'new' template" do
        BoatTotalOwner.stub(:new) { mock_boat_total_owner(:save => false) }
        post :create, :boat_total_owner => {}
        response.should render_template("new")
      end
    end
  end

  describe "PUT update" do
    describe "with valid params" do
      it "updates the requested boat_total_owner" do
        BoatTotalOwner.stub(:find).with("37") { mock_boat_total_owner }
        mock_boat_total_owner.should_receive(:update_attributes).with({'these' => 'params'})
        put :update, :id => "37", :boat_total_owner => {'these' => 'params'}
      end

      it "assigns the requested boat_total_owner as @boat_total_owner" do
        BoatTotalOwner.stub(:find) { mock_boat_total_owner(:update_attributes => true) }
        put :update, :id => "1"
        assigns(:boat_total_owner).should be(mock_boat_total_owner)
      end

      it "redirects to the boat_total_owner" do
        BoatTotalOwner.stub(:find) { mock_boat_total_owner(:update_attributes => true) }
        put :update, :id => "1"
        response.should redirect_to(boat_total_owner_url(mock_boat_total_owner))
      end
    end

    describe "with invalid params" do
      it "assigns the boat_total_owner as @boat_total_owner" do
        BoatTotalOwner.stub(:find) { mock_boat_total_owner(:update_attributes => false) }
        put :update, :id => "1"
        assigns(:boat_total_owner).should be(mock_boat_total_owner)
      end

      it "re-renders the 'edit' template" do
        BoatTotalOwner.stub(:find) { mock_boat_total_owner(:update_attributes => false) }
        put :update, :id => "1"
        response.should render_template("edit")
      end
    end
  end

  describe "DELETE destroy" do
    it "destroys the requested boat_total_owner" do
      BoatTotalOwner.stub(:find).with("37") { mock_boat_total_owner }
      mock_boat_total_owner.should_receive(:destroy)
      delete :destroy, :id => "37"
    end

    it "redirects to the boat_total_owners list" do
      BoatTotalOwner.stub(:find) { mock_boat_total_owner }
      delete :destroy, :id => "1"
      response.should redirect_to(boat_total_owners_url)
    end
  end

end

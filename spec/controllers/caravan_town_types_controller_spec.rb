require 'spec_helper'

# This spec was generated by rspec-rails when you ran the scaffold generator.
# It demonstrates how one might use RSpec to specify the controller code that
# was generated by the Rails when you ran the scaffold generator.

describe CaravanTownTypesController do

  def mock_caravan_town_type(stubs={})
    @mock_caravan_town_type ||= mock_model(CaravanTownType, stubs).as_null_object
  end

  describe "GET index" do
    it "assigns all caravan_town_types as @caravan_town_types" do
      CaravanTownType.stub(:all) { [mock_caravan_town_type] }
      get :index
      assigns(:caravan_town_types).should eq([mock_caravan_town_type])
    end
  end

  describe "GET show" do
    it "assigns the requested caravan_town_type as @caravan_town_type" do
      CaravanTownType.stub(:find).with("37") { mock_caravan_town_type }
      get :show, :id => "37"
      assigns(:caravan_town_type).should be(mock_caravan_town_type)
    end
  end

  describe "GET new" do
    it "assigns a new caravan_town_type as @caravan_town_type" do
      CaravanTownType.stub(:new) { mock_caravan_town_type }
      get :new
      assigns(:caravan_town_type).should be(mock_caravan_town_type)
    end
  end

  describe "GET edit" do
    it "assigns the requested caravan_town_type as @caravan_town_type" do
      CaravanTownType.stub(:find).with("37") { mock_caravan_town_type }
      get :edit, :id => "37"
      assigns(:caravan_town_type).should be(mock_caravan_town_type)
    end
  end

  describe "POST create" do
    describe "with valid params" do
      it "assigns a newly created caravan_town_type as @caravan_town_type" do
        CaravanTownType.stub(:new).with({'these' => 'params'}) { mock_caravan_town_type(:save => true) }
        post :create, :caravan_town_type => {'these' => 'params'}
        assigns(:caravan_town_type).should be(mock_caravan_town_type)
      end

      it "redirects to the created caravan_town_type" do
        CaravanTownType.stub(:new) { mock_caravan_town_type(:save => true) }
        post :create, :caravan_town_type => {}
        response.should redirect_to(caravan_town_type_url(mock_caravan_town_type))
      end
    end

    describe "with invalid params" do
      it "assigns a newly created but unsaved caravan_town_type as @caravan_town_type" do
        CaravanTownType.stub(:new).with({'these' => 'params'}) { mock_caravan_town_type(:save => false) }
        post :create, :caravan_town_type => {'these' => 'params'}
        assigns(:caravan_town_type).should be(mock_caravan_town_type)
      end

      it "re-renders the 'new' template" do
        CaravanTownType.stub(:new) { mock_caravan_town_type(:save => false) }
        post :create, :caravan_town_type => {}
        response.should render_template("new")
      end
    end
  end

  describe "PUT update" do
    describe "with valid params" do
      it "updates the requested caravan_town_type" do
        CaravanTownType.stub(:find).with("37") { mock_caravan_town_type }
        mock_caravan_town_type.should_receive(:update_attributes).with({'these' => 'params'})
        put :update, :id => "37", :caravan_town_type => {'these' => 'params'}
      end

      it "assigns the requested caravan_town_type as @caravan_town_type" do
        CaravanTownType.stub(:find) { mock_caravan_town_type(:update_attributes => true) }
        put :update, :id => "1"
        assigns(:caravan_town_type).should be(mock_caravan_town_type)
      end

      it "redirects to the caravan_town_type" do
        CaravanTownType.stub(:find) { mock_caravan_town_type(:update_attributes => true) }
        put :update, :id => "1"
        response.should redirect_to(caravan_town_type_url(mock_caravan_town_type))
      end
    end

    describe "with invalid params" do
      it "assigns the caravan_town_type as @caravan_town_type" do
        CaravanTownType.stub(:find) { mock_caravan_town_type(:update_attributes => false) }
        put :update, :id => "1"
        assigns(:caravan_town_type).should be(mock_caravan_town_type)
      end

      it "re-renders the 'edit' template" do
        CaravanTownType.stub(:find) { mock_caravan_town_type(:update_attributes => false) }
        put :update, :id => "1"
        response.should render_template("edit")
      end
    end
  end

  describe "DELETE destroy" do
    it "destroys the requested caravan_town_type" do
      CaravanTownType.stub(:find).with("37") { mock_caravan_town_type }
      mock_caravan_town_type.should_receive(:destroy)
      delete :destroy, :id => "37"
    end

    it "redirects to the caravan_town_types list" do
      CaravanTownType.stub(:find) { mock_caravan_town_type }
      delete :destroy, :id => "1"
      response.should redirect_to(caravan_town_types_url)
    end
  end

end

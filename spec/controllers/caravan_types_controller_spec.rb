require 'spec_helper'

# This spec was generated by rspec-rails when you ran the scaffold generator.
# It demonstrates how one might use RSpec to specify the controller code that
# was generated by the Rails when you ran the scaffold generator.

describe CaravanTypesController do

  def mock_caravan_type(stubs={})
    @mock_caravan_type ||= mock_model(CaravanType, stubs).as_null_object
  end

  describe "GET index" do
    it "assigns all caravan_types as @caravan_types" do
      CaravanType.stub(:all) { [mock_caravan_type] }
      get :index
      assigns(:caravan_types).should eq([mock_caravan_type])
    end
  end

  describe "GET show" do
    it "assigns the requested caravan_type as @caravan_type" do
      CaravanType.stub(:find).with("37") { mock_caravan_type }
      get :show, :id => "37"
      assigns(:caravan_type).should be(mock_caravan_type)
    end
  end

  describe "GET new" do
    it "assigns a new caravan_type as @caravan_type" do
      CaravanType.stub(:new) { mock_caravan_type }
      get :new
      assigns(:caravan_type).should be(mock_caravan_type)
    end
  end

  describe "GET edit" do
    it "assigns the requested caravan_type as @caravan_type" do
      CaravanType.stub(:find).with("37") { mock_caravan_type }
      get :edit, :id => "37"
      assigns(:caravan_type).should be(mock_caravan_type)
    end
  end

  describe "POST create" do
    describe "with valid params" do
      it "assigns a newly created caravan_type as @caravan_type" do
        CaravanType.stub(:new).with({'these' => 'params'}) { mock_caravan_type(:save => true) }
        post :create, :caravan_type => {'these' => 'params'}
        assigns(:caravan_type).should be(mock_caravan_type)
      end

      it "redirects to the created caravan_type" do
        CaravanType.stub(:new) { mock_caravan_type(:save => true) }
        post :create, :caravan_type => {}
        response.should redirect_to(caravan_type_url(mock_caravan_type))
      end
    end

    describe "with invalid params" do
      it "assigns a newly created but unsaved caravan_type as @caravan_type" do
        CaravanType.stub(:new).with({'these' => 'params'}) { mock_caravan_type(:save => false) }
        post :create, :caravan_type => {'these' => 'params'}
        assigns(:caravan_type).should be(mock_caravan_type)
      end

      it "re-renders the 'new' template" do
        CaravanType.stub(:new) { mock_caravan_type(:save => false) }
        post :create, :caravan_type => {}
        response.should render_template("new")
      end
    end
  end

  describe "PUT update" do
    describe "with valid params" do
      it "updates the requested caravan_type" do
        CaravanType.stub(:find).with("37") { mock_caravan_type }
        mock_caravan_type.should_receive(:update_attributes).with({'these' => 'params'})
        put :update, :id => "37", :caravan_type => {'these' => 'params'}
      end

      it "assigns the requested caravan_type as @caravan_type" do
        CaravanType.stub(:find) { mock_caravan_type(:update_attributes => true) }
        put :update, :id => "1"
        assigns(:caravan_type).should be(mock_caravan_type)
      end

      it "redirects to the caravan_type" do
        CaravanType.stub(:find) { mock_caravan_type(:update_attributes => true) }
        put :update, :id => "1"
        response.should redirect_to(caravan_type_url(mock_caravan_type))
      end
    end

    describe "with invalid params" do
      it "assigns the caravan_type as @caravan_type" do
        CaravanType.stub(:find) { mock_caravan_type(:update_attributes => false) }
        put :update, :id => "1"
        assigns(:caravan_type).should be(mock_caravan_type)
      end

      it "re-renders the 'edit' template" do
        CaravanType.stub(:find) { mock_caravan_type(:update_attributes => false) }
        put :update, :id => "1"
        response.should render_template("edit")
      end
    end
  end

  describe "DELETE destroy" do
    it "destroys the requested caravan_type" do
      CaravanType.stub(:find).with("37") { mock_caravan_type }
      mock_caravan_type.should_receive(:destroy)
      delete :destroy, :id => "37"
    end

    it "redirects to the caravan_types list" do
      CaravanType.stub(:find) { mock_caravan_type }
      delete :destroy, :id => "1"
      response.should redirect_to(caravan_types_url)
    end
  end

end
